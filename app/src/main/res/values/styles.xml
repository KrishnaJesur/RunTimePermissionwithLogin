<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="PoppinsRegular" parent="@android:style/TextAppearance">
        <item name="android:fontFamily">@font/poppins_regular</item>
        <item name="android:textColor">@color/pureBlack</item>
        <item name="android:textSize">12sp</item>
        <item name="colorControlNormal">@color/pureBlack</item>
        <item name="colorControlActivated">@color/pureBlack</item>
        <item name="colorControlHighlight">@color/pureBlack</item>
    </style>

    <style name="PoppinsRegularRadioButton" parent="@android:style/TextAppearance">
        <item name="android:fontFamily">@font/poppins_regular</item>
        <item name="android:textColor">@color/pureBlack</item>
        <item name="android:textSize">12sp</item>
        <item name="colorControlNormal">@color/pureBlack</item>
        <item name="colorControlActivated">@color/pureBlack</item>
        <item name="colorControlHighlight">@color/pureBlack</item>
        <item name="android:buttonTint">@color/colorPrimaryDark</item>
    </style>

    <style name="PoppinsRegularTextInputLayout" parent="Widget.Design.TextInputLayout">
        <item name="android:fontFamily">@font/poppins_regular</item>
        <item name="android:textColor">@color/pureBlack</item>
        <item name="android:textSize">12sp</item>
        <item name="colorControlNormal">@color/pureBlack</item>
        <item name="colorControlActivated">@color/pureBlack</item>
        <item name="colorControlHighlight">@color/pureBlack</item>
    </style>


</resources>
